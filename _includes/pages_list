{% for node in pages_list %}
	{% if group == null or group == node.group %}
		{% if page.url == node.url %}
			<li>
                <a href="{{ node.url }}" class="active-tab">{{ node.menu }}</a>
            </li>
		{% else %}
			<li>
                <a href="{{ node.url }}"">{{ node.menu }}</a>
            </li>
		{% endif %}
	{% endif %}
{% endfor %}
{% assign pagis_list = nil %}
{% assign group = nil %}